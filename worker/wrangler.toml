name = "zlipper-webhook"
main = "worker.js"
compatibility_date = "2026-08-01"

[vars]
GITHUB_REPO = "ootvai/zlipper"

# Kaikki muu asetetaan secreteina, EI tähän tiedostoon — myös chat id,
# jotta sitä ei tarvitse committoida julkiseen repoon:
#   wrangler secret put TELEGRAM_BOT_TOKEN
#   wrangler secret put TELEGRAM_WEBHOOK_SECRET
#   wrangler secret put GITHUB_TOKEN
#   wrangler secret put ALLOWED_CHAT_ID
#
# Helpoiten: aja .\setup.ps1, joka tekee nämä ja rekisteröi webhookin.

# Klippivahdin ajastin. GitHubin oma cron ei laukea luvatusti (mitattu:
# mediaani 42,7 min, pisin katkos 6 h), joten oikea ajastin on taalla ja
# GitHubin schedule-lohko jaa varajarjestelmaksi. Ilmaisplanilla saa 5
# cron-triggeria per tili; tama on niista yksi.
[triggers]
crons = ["*/10 * * * *"]
